{
  "name": "sbb-monorepo",
  "private": true,
  "workspaces": [
    "webservice",
    "webapp"
  ],
  "devDependencies": {
    "lerna": "^3.13.1"
  },
  "scripts": {
      "postinstall": "yarn bootstrap",
      "bootstrap": "lerna bootstrap --use-workspaces",
      "dev": "lerna run --parallel dev",
      "test": "lerna run test"
  }
}
